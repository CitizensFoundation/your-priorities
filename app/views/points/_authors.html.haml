%p
  .authors_heading= tr('Authors', 'view/documents/_authors')
  - for user in users
    %table{:border => "0", :cellpadding => "0", :cellspacing => "0", :class => "authors_table"}
      %tr
        %td{:valign => "top", :width => "24"}
          = render :partial => "users/pic", :locals => {:user => user[0]}
        %td{:style => "padding-left: 5px;", :valign => "top"}
          - if user[0].status == 'suspended' or user[0].status == 'removed'
            <strike>
          = link_to user[0].name, user[0]
          %span.pc= tr("{capital}{currency_short_name}", "view/documents/_authors", :capital => user[0].capitals_count, :currency_short_name => current_instance.currency_short_name)
          - if user[0].status == 'suspended' or user[0].status == 'removed'
            </strike>
          .profile_small= tr("{count} revisions", "view/documents/_authors", :count => user[1])
